<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Створити зарплату</title>
  <link rel="stylesheet" href="/create-salary-styles.css">
</head>
<body>
  <div class="container">
    <h1>Створити зарплату</h1>
    <form action="/admin/salaries" method="POST">
      <div class="form-group">
        <label for="userId">Користувач:</label>
        <select id="userId" name="userId" required>
          <% users.forEach(user => { %>
            <option value="<%= user._id %>"><%= user.name %></option>
          <% }) %>
        </select>
      </div>
      <div class="form-group">
        <label for="amount">Сума:</label>
        <input type="number" id="amount" name="amount" required>
      </div>
      <div class="form-group">
        <label for="date">Дата:</label>
        <input type="date" id="date" name="date" required>
      </div>
      <button type="submit">Створити</button>
    </form>
    <a href="/admin/salaries">Назад до зарплат</a>
  </div>
</body>
</html>
